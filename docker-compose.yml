version: "3.9"

services:
  bot:
    build: .
    container_name: task_tracker_bot
    restart: unless-stopped
    env_file: .env
    volumes:
      - ./data:/app/data
    # Раскомментируй для просмотра логов в реальном времени:
    # logging:
    #   driver: "json-file"
    #   options:
    #     max-size: "10m"
    #     max-file: "3"
